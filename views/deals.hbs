<!--   Big container   -->
<div> <a href="/{{newdealpagelink}}" class="btn-success btn">Add New Deal</a></div>

<div class="card">
    <div class="row">
    <div class="card-header" data-background-color="purple">
                    <h4 class="title">{{title}}</h4>
                    
                </div>
        <div class="col-sm-12">
            <!--      Wizard container        -->
            {{#if results}}
                <table class="table responsive">
                    <tr><th>Deal Subcategory</th><th>Deal Store</th><th>Deal Store Address</th><th>Deal City</th><th>Deal State</th><th>Deal Offer</th><th>Action</th></tr>
                {{#each results}}
                   <tr><td> {{dealSubcategory}}</td><td> {{dealStore}}</td><td> {{dealStoreAddress}}</td><td> {{dealCity}}</td><td> {{dealState}}</td><td> {{dealOffer}}</td><td><a href="/updatedeal?dealid={{dealId}}" class=" text-success">Update</a><a href="#"  data-toggle="modal" data-id={{dealId}} data-target="#myModal" onclick="showdeletepopup(this)"  class="deletedeal text-danger pull-right">Delete</a></td></tr>
                {{/each}}
                </table>
            {{else}}
                <div class="col-lg-12">
                    <h3>No deals found.</h3>
                </div>
            {{/if}}
        </div>
    </div>
    <!-- end row -->
</div>
<!--  big container -->
<style>
.modal-backdrop{position:inherit;}
</style>
  <!-- Modal -->
  <div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog modal-sm">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">&times;</button>
          <h4 class="modal-title">Delete Deal</h4>
        </div>
        <div class="modal-body">
          <p>Are you sure, you want to delete this deal?</p>
        </div>
        <div class="modal-footer">
          <a href="" id="deletedeal" class="btn btn-default">Delete</a>
        </div>
      </div>
    </div>
  </div>
</div>
